spring:
  application:
    name: quartz-service
  profiles:
    active: dev

server:
  port: 5007
#
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:5000/eureka/


#feign:
#  hystrix:
#    enabled: true
logging:
  level:
    com:
      cloud:
        quartz:
          mapper: debug